<nav id="nav" class="nav">
	<div class="nav-container">
		{% if app.user %}

			<button id="avatar" class="avatar">
				<div class="avatar-container">
					<img src="{{ asset('img/profile/user-1.jpg') }}" alt="">
				</div>
			</button>

			<ul id="navMenu" class="nav-list">
				<li class="nav-list_item">
					<a href="{{ path('app_profile') }}">Profile</a>
				</li>
				<li class="nav-list_item">
					<a href="{{ path('app_explore') }}">Explore</a>
				</li>
        {% if is_granted('ROLE_ADMIN') %} 
            <li class="nav-list_item admin">
              <a href="{{ path('app_blog_index') }}">Blogs</a>
            </li>
            <li class="nav-list_item admin">
              <a href="#">Users</a>
              {# <a href="{{ path('app_users') }}">Users</a> #}
            </li>
        {% endif %}
				<li class="nav-list_item">
					<a href="{{ path('app_logout') }}">Logout</a>
				</li>
			</ul>

		{% else %}

			<a class="nav-link" href="{{ path('app_login') }}">
				<button class='btn'>SIGN IN</button>
			</a>
			<a class="nav-link" href="{{ path('app_explore') }}">
				<button class='btn btn_sec'>explore</button>
			</a>
		{% endif %}
	</div>
</nav>

